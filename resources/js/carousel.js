$(document).ready(function(){
    $('.info22').slick({
        slidesToShow: 1, // Mostrar apenas 1 slide por vez
        slidesToScroll: 1, // Número de slides a rolar por vez
        autoplay: true, // Iniciar o carrossel automaticamente
        autoplaySpeed: 2000 // Velocidade do carrossel em milissegundos (opcional)
    });
});
